@model FactionFraction.Models.GroupMember
@{
    ViewData["Title"] = "Details";
}
<h1>@Model.Name <p class="pull-right small">Desired Grade: @Html.DisplayFor(model => model.DesiredGrade)</p></h1>
<div class="well-lg">
    <div class="container-fluid">
        <div class="row">
            @*@if (Model.AssignedTasks.Count() > 0)
            {
                @foreach (var t in Model.AssignedTasks)
                {


                }
            }*@
        </div>
    </div>
</div>
<div>
    <a class="btn btn-success" asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a class="btn btn-info" asp-action="Index">Back to List</a>
</div>
